<p id="notice"><%= notice %></p>

<h1>Coments</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>Post</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @coments.each do |coment| %>
      <tr>
        <td><%= coment.content %></td>
        <td><%= coment.post %></td>
        <td><%= coment.user %></td>
        <% if current_user.admin == true || current_user.id == coment.user.id %>
            <td><%= link_to 'Edit', edit_post_path(post)%></td>
            <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% elsif current_user.id == coment.post.user.id %>
            <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
        
        <td><%= link_to 'Show', coment %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Coment', new_coment_path %>
